[defaults]
organisation = "Paperforge"
publisher = "Tim Riset Paperforge"

[[collection]]
slug = "id-project"
root = "."
profile = "none"                 # no shipped profile for Indonesian
profile_file = "profile.toml"    # the team supplies the whole declaration

  [[collection.document]]
  source = "report.md"
  output = "report.html"
  contents_heading = "DAFTAR ISI"
  page_numbers = true
  publish = false

  # `note` is a brief that does not number its pages. It is a thin composition
  # of two covered types, which is exactly why nothing exercised it until the
  # capability gate asked - see unit_gates.
  [[collection.document]]
  source = "report.md"
  type = "note"
  output = "note.html"
  publish = false

[internal]
files = []
reason = "demo"
